name: portable-rustls CI

permissions:
  contents: read

on:
  push:
    branches-ignore:
    - 'gh-readonly-queue/**'
    tags:
    - '**'
  pull_request:
  merge_group:
  schedule:
    - cron: '0 18 * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: build - portable_atomic_util::Arc # (via critical-section feature)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        rust:
          - nightly
          - nightly-2024-01-01
          # RUST NIGHTLY version(s) for MSRV - 1.71:
          - nightly-2023-06-01
          # BUILD FAILS with older Rust nightly:
          # - nightly-2023-05-01
        os:
          - ubuntu-latest
          # FOR FUTURE CONSIDERATION:
          # - macos-latest
          # - windows-latest
        target:
          # 32-BIT TARGET(S) WITH NO ATOMIC PTR:
          - riscv32imc-unknown-none-elf
          # 64-bit target(s) with no std crate - for comparison:
          - aarch64-unknown-none
          - x86_64-unknown-none
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install ${{ matrix.rust }} toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          target: ${{ matrix.target }}

      - name: cargo build (debug; no-std; critical-section) # (with no built-in provider)
        run: cargo build --features critical-section --target ${{ matrix.target }}
        working-directory: rustls
        env:
          # (unstable cfg flag required for critical-section feature)
          RUSTFLAGS: --cfg portable_atomic_unstable_coerce_unsized

      # NOTE: BUILD with hashbrown FAILS with no atomic ptr - MISSING SUPPORT in default hasher
      # - name: cargo build (debug; no-std; hashbrown) # (with no built-in provider)
      #   ...

  test:
    # GENERAL NOTE: NO CI TESTING for no-std
    # (CI TESTING with no-std & with no atomic ptr FOR FUTURE CONSIDERATION)
    name: test - portable_atomic_util::Arc # (via critical-section feature)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        rust:
          - nightly
          - nightly-2024-05-01
          # FAILS cargo test: use of unstable library feature in proc-macro2
          # - nightly-2024-04-01
        os:
          - ubuntu-latest
          - macos-latest
          # FOR FUTURE CONSIDERATION:
          # - windows-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install ${{ matrix.rust }} toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          target: ${{ matrix.target }}

      - name: cargo test (debug; std; critical-section) # (with no built-in provider)
        run: cargo test --features std,critical-section
        working-directory: rustls
        env:
          # (unstable cfg flag required for critical-section feature)
          RUSTFLAGS: --cfg portable_atomic_unstable_coerce_unsized

  cross-build:
    name: cross build test - portable_atomic_util::Arc # (via critical-section feature)
    runs-on: ${{ matrix.os }}
    if: github.event_name != 'merge_group'
    strategy:
      matrix:
        rust:
          - nightly
          - nightly-2024-07-01
          # FAILS to install cross (cross-rs) from GitHub
          # - nightly-2024-06-01
        os:
          - ubuntu-latest
          # FOR FUTURE CONSIDERATION:
          # - macos-latest
          # - windows-latest
        target:
          # 32-bit Android (Linux) target(s) - for comparison:
          - armv7-linux-androideabi
          # 32-BIT TARGET(S) WITH NO ATOMIC PTR:
          - thumbv6m-none-eabi
          # additional target(s) - for comparison:
          # NOTE: This could have some overlap with 64-bit ARM-style CPU on macOS CI host;
          # may have similar ARM-style CPU overlap with standard Windows & possibly Linux in the future
          - aarch64-linux-android
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install ${{ matrix.rust }} toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          target: ${{ matrix.target }}
      - name: Install cross (cross-rs) from GitHub
        run: cargo install cross --git https://github.com/cross-rs/cross
      - run: cross build --package portable-rustls --features critical-section --target ${{ matrix.target }}
        env:
          # (unstable cfg flag required for critical-section feature)
          RUSTFLAGS: --cfg portable_atomic_unstable_coerce_unsized

  cross-test:
    # GENERAL NOTE: NO CI TESTING for no-std
    # (CI TESTING with no-std & with no atomic ptr FOR FUTURE CONSIDERATION)
    name: cross-target testing - portable_atomic_util::Arc # (via critical-section feature)
    runs-on: ${{ matrix.os }}
    if: github.event_name != 'merge_group'
    strategy:
      matrix:
        rust:
          - nightly
          - nightly-2024-07-01
          # FAILS to install cross (cross-rs) from GitHub
          # - nightly-2024-06-01
        target:
          # 32-bit Android (Linux) targets:
          - armv7-linux-androideabi
          - i686-linux-android
          - thumbv7neon-linux-androideabi
          # additional target(s):
          # NOTE: This could have some overlap with 64-bit ARM-style CPU on macOS CI host;
          # may have similar ARM-style CPU overlap with standard Windows & possibly Linux in the future
          - aarch64-linux-android
        os:
          - ubuntu-latest
          # FOR FUTURE CONSIDERATION:
          # ...
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install ${{ matrix.rust }} toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          target: ${{ matrix.target }}
      - name: Install cross (cross-rs) from GitHub
        run: cargo install cross --git https://github.com/cross-rs/cross
      - name: Install bindgen feature & CLI for aws-lc-sys (as needed for many cross targets)
        run: cargo add --dev --features bindgen 'aws-lc-sys@>0.20' --package portable-rustls --verbose && cargo install bindgen-cli --verbose
      - run: cross test --features critical-section --package portable-rustls --target ${{ matrix.target }}
        env:
          # (unstable cfg flag required for critical-section feature)
          RUSTFLAGS: --cfg portable_atomic_unstable_coerce_unsized
      - run: cross test --all-features --package portable-rustls --target ${{ matrix.target }}
        env:
          # unstable cfg flag required for critical-section feature (as implied by --all-features)
          RUSTFLAGS: --cfg portable_atomic_unstable_coerce_unsized
